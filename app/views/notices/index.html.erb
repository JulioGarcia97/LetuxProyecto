<% @notices.each do |notice| %>
	<div class="index_row clearfix">
    <p class="todo_list_title"><%= link_to notice.title, notice_path(notice) %></p>
  </div>
<% end %>

<% if logged_in?(:site_admin) %>
<div class="links">
	<%= link_to "Nuevo Aviso", new_notice_path %>
</div>
<% end %>